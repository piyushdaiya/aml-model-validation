# Default local setup uses Docker Compose for PostgreSQL on host port 5433.
DATABASE_URL="postgresql://amluser:amlpassword@localhost:5433/aml_db?schema=public"

# Replace in production or shared environments.
JWT_SECRET="replace-with-a-long-random-secret"
